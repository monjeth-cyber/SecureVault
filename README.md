# SecureVault
This laboratory exercise is about creating a secure identity system using Flutter. In this project, we applied the Strict MVVM (Model-View-ViewModel) architecture to properly separate the user interface, business logic, and services
